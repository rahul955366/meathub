# Task TODO (Vercel deploy)

- [x] Run `npm run build` in `frontend/` and fix/confirm build passes.
- [x] Commit local changes (Next version bump) to Git.
- [x] Push latest code to GitHub (`origin/main`).
- [ ] Create Vercel project from GitHub repo.
- [ ] Configure framework/build settings to use `frontend/` as root.
- [ ] Deploy to Vercel and retrieve public URL.

